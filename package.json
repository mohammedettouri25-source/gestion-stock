{
  "name": "gstock-monolith",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build && cd ../backend && composer install --no-dev --optimize-autoloader"
  }
}
